<%= form_with model: [@restaurant, @fooditem , @cart] do |f| %>
  <%= f.hidden_field :name, value: cuisine.name %>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <%= f.hidden_field :fooditem_id, value: fooditem.id %>
  <%= f.submit "Add to Cart" %>
<% end %>