<%= form_with model: [@restaurant, @cuisine , @cart] do |f| %>
  <%= f.hidden_field :name, value: cuisine.name %>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <%= f.hidden_field :cuisine_id, value: cuisine.id %>
  <%= f.submit "Add to Cart" %>
<% end %>